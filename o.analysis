-- File 3: Order Items Analysis

SELECT 
  item_id,
  order_id,
  product_id,
  quantity,
  unit_price,
  (quantity * unit_price) AS line_total,
  (quantity * unit_price) AS metric
FROM order_items
ORDER BY order_id;
